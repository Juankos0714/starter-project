rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
<<<<<<< HEAD
    //TODO: WRITE ANY NECESSARY STORAGE RULES HERE
    }
}
=======
    match /media/articles/{imageId} {
      allow read: if true;
      // Emulator: open for unauthenticated writes during local testing.
      // Production: restore to:
      //   allow write: if request.auth != null
      //                && request.resource.size < 5 * 1024 * 1024
      //                && request.resource.contentType.matches('image/.*');
      allow write: if true;
    }
  }
}
>>>>>>> fbce432 (Finish PR (project setup))
